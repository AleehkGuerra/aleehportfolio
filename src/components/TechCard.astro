---
export interface Props{
    label:string,
    icon:string,
}
const{label,icon} = Astro.props
---
<div class="flex flex-col gap-4 bg-slate-600 items-center rounded-lg p-2 border-2 border-violet-500">
 <img src={icon} class="size-16 rounded-xl " >
 <label >{label}</label>
</div>